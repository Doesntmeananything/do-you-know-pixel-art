import React from "react"
import backgroundLayerStyles from "./background-layer.module.css"

const BackgroundLayer = props => (
  <div className={backgroundLayerStyles.background} />
)

export default BackgroundLayer
