import React from "react"

// A heading with the year for a paticular slide
// A flexbox with two parts:
// 1. A heading and a paragraph
// 2. Image carousel

const Slide = ({ slideYear }) => <header>{slideYear}</header>
